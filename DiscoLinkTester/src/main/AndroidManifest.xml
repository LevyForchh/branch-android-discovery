<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.branch.search.linktest">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="io.branch.search.linktest.DiscoLinkTesterApp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="io.branch.search.linktest.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppThemeNoTitle"
            android:windowSoftInputMode="adjustPan">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- The demo Branch Key.  Please replace this key with your own key before release. -->
        <meta-data android:name="io.branch.sdk.BranchKey" android:value="key_live_jgKhogSvbj1sZ7PJiQ47dfkozAmfeA4H" />

    </application>
</manifest>
